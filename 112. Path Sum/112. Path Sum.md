## Step.0
まずは自力で解く  
根から葉までの値の総和が`targetSum`と一致するところがあるかどうかを答える  

これまでの問題を経て二分木にも結構慣れてきた  
解法もDFS(iterative or recursion), BFS(iterative)の3通りで考えてみる  
葉まで道中の値を足しながら探索していって、最終的に一致するかを見てあげればよい　　

最初に思いついたのはDFS × iterative  
アイデアは上に書いた通り  

和を取るとき毎回思うのだが、変数名は`total`で問題ないのだろうか...？  
`sum`は関数名ですでに存在しているから紛らわしい  

2分くらいで書けた

### 書いたコード1 (DFS × iterative)
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def hasPathSum(self, root: Optional[TreeNode], targetSum: int) -> bool:
        if root is None:
            return False

        node_and_total = [(root, 0)]

        while node_and_total:
            node, total = node_and_total.pop()
            total += node.val

            if node.left is None and node.right is None:
                if total == targetSum:
                    return True

            if node.left is not None:
                node_and_total.append((node.left, total))
            if node.right is not None:
                node_and_total.append((node.right, total))
        
        return False
```
時間計算量：`O(N)` 最悪の場合、すべてのパスを見る必要があるため、入力のノード数に比例  
空間計算量：`O(N)` 同上  

スタックをキューに変えれば、BFS × iterativeをすぐに書ける  

### 書いたコード2 (BFS × iterative)
```python
from collections import deque

# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def hasPathSum(self, root: Optional[TreeNode], targetSum: int) -> bool:
        if root is None:
            return False

        node_and_total = deque([(root, 0)])

        while node_and_total:
            node, total = node_and_total.popleft()
            total += node.val

            if node.left is None and node.right is None:
                if total == targetSum:
                    return True

            if node.left is not None:
                node_and_total.append((node.left, total))
            if node.right is not None:
                node_and_total.append((node.right, total))
        
        return False
```

ただ、再帰による解法が思いつかない  
ヘルパー関数を用いて、これまで見てきたノードの値の和を保持できれば書けるかもしれないけど、  
わざわざヘルパー関数を使うのはどうなのか  
とりあえずヘルパー関数を使用する形で書いてみる  

### 書いたコード3 (DFS × recursion)
```python
from collections import deque

# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def hasPathSum(self, root: Optional[TreeNode], targetSum: int) -> bool:
        if root is None:
            return False

        total = 0
        def explore_total(node, total):
            if node is None:
                return False
            
            total += node.val
            if node.left is None and node.right is None:
                return total == targetSum
            
            return explore_total(node.left, total) or explore_total(node.right, total)
        
        return explore_total(root, total)
```
実行したところ、コード1, 2が`0ms`に対して、コード3は`3ms`という結果だった  
`explore_total`を再起呼び出しする分、時間がかかる  

## Step.1  
他の方々のPRを見て勉強する  
全体的に、どの方も自分と同じような解法3通りで試していた  

https://github.com/yumyum116/LeetCode_Arai60/blob/112-path-sum/112-path-sum/memo.md  
保持する値を目標値の方に設定する方法を試されていた  

### 書いたコード4
```python
from collections import deque

# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def hasPathSum(self, root: Optional[TreeNode], targetSum: int) -> bool:
        if root is None:
            return False
        
        if root.left is None and root.right is None:
            return root.val == targetSum
        
        remainingSum = targetSum - root.val

        return self.hasPathSum(root.left, remainingSum) or self.hasPathSum(root.right, remainingSum)
```

## Step.2
コード1~3を、見ずにミスなく3回書く



