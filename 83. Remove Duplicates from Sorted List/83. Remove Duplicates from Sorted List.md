```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        current_node = head
        previous_node = None
        visited_val = set()

        while current_node:
            if current_node.val in visited_val:
                previous_node.next = current_node.next
            
            else:
                visited_val.add(current_node.val)
                previous_node = current_node

            current_node = current_node.next
        return head
```

感想

エラーが出る、答えが一致しないなどでかなり試行錯誤しました。(30分ほど)

エラー理由としては返り値を`head`ではなく`current_node`など指しているノード単体にしていたこと。

アイデアとしては、前から順にノードを見ていき、重複箇所があったら、そのノードを飛ばして前後のノードを接続するというものです。

全問での指摘で`current`を変数名に使うときには「現在」という意味が必要になるときにすべきとあったのですが、

この問題では現在指すノードと、その一つ前に指すノードの2つを区別する必要があったため、`current_node`,`previous_node`を使用しています。

`current_node`の値がすでに`visited_val`に存在する(=重複する)時に、`current_node`を飛ばしてノードをつなげること重複削除をしています。

少し気になったのですが、変数名は`current_node`,`previous_node`ではなく、`curr_node`,`prev_node`の方が良いのでしょうか...？
