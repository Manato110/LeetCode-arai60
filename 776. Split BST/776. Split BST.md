### 問題
LeetCode ver: https://leetcode.com/problems/split-bst/description/  
LintCode ver: https://www.lintcode.com/problem/847/  

## Step.0

まずは自力で解く  
問題の理解から  
Description  
Given a binary search tree (BST) with a root node root and a target value v, the tree is partitioned into two subtrees, one of which has nodes that are both less than or equal to the target value and the other has nodes that are both greater than the target value, where the nodes of the given binary search tree root do not necessarily have nodes with values equal to v.

In addition, most of the structure of the original tree root should be preserved. Simply put, for any child node C that has a parent node P in the original tree, if they are both in the same subtree after the split, then the node C should still be a child node of P.

For the two split subtrees, the one with the highest number of nodes is returned, or if the two subtrees have the same number of nodes, the one with the largest root node value is returned.  


Example
```python 
Example 1:

Input:
root = {4,2,6,1,3,5,7}
v = 2
Output:
{4,3,6,#,#,5,7}
Explanation:
The given tree {4,2,6,1,3,5,7} is represented as the following:

          4
        /   \
       2     6
      / \   / \
     1   3 5   7

The two subtrees after splitting are:

          4
        /   \
      3      6      and    2
            / \           /
           5   7         1

The subtree with 4 as the root node has more nodes, so it returns the left subtree.
Example 2:

Input:
root = {5,2,7,1,3,6}
v = 4
Output:
{5,#,7,6}
Explanation:
The given tree {5,2,7,1,3,6} is represented as the following:

         5
       /   \
      2     7
     / \   / 
    1   3 6   

The two subtrees after splitting are:

         2                  5
       /   \                 \
      1     3     and         7
                             /
                            6
```
Constraint  
ノード数は50以下  
全てのノードを見る余裕はありそう  

二分探索木とターゲット値が与えられる  
ターゲット以下とターゲットより大きい値の木を分割し、ノード数が大きい方を返す  
分割後のノード数が同じなら、根の値が大きい方を返す  
分割するときに、元の木構造はなるべく崩さないようにしないといけない  


実際に書いて手で動かすならわかりやすい、が、アルゴリズムに落とし込むのはなかなか難しそうに感じる  
一番簡単にできるのは、二分探索木を一通り探索して、  
ターゲット値より大きいノードでまとめた木(`upper`)、ターゲット値以下のノードでまとめた木(`lower`)を構築する方法  

ノードを見て、その値がターゲット値以下か否かを判定し、`upper`ないし`lower`に振り分ける  
その後、ノードの左の子・右の子について再帰的に見ていく...としてみる  

恐らく、ターゲット値とノードの値の関係から、全てのノードを見なくても、一部どちらに属するかを判断することが出来て、  
若干早く実行を終えることができるかもしれない  

いきなりそのあたりの分岐要素を組み込むのは能力的にきついと感じたので、まずは動くコードを書いてみようと思う  
全てのノードを1回ずつ見るので、時間計算量は`O(n)`、空間計算量は`O(n)`になる  

書こうとしているときに気づいたが、分けた後に、ノードの数を比較するのはどうしたらよいか。  
各木をそれぞれ見てノード数を数え直すか、分けるときにノードの数の情報も計算しておくか  

一旦、分けた後に数える方向でやってみる  
こちらの方が時間的に手間がかかってしまうが、実装しやすいと考えた  

最後に、どちらの木を出力するかについて、ノードの個数が`lower`の方が多いならこちらを  
それ以外の場合なら`upper`が出力されるようにする  

コードに起こしてみる  
### 書いたコード 1
```python
from lintcode import (
    TreeNode,
)

"""
Definition of TreeNode:
class TreeNode:
    def __init__(self, val):
        self.val = val
        self.left, self.right = None, None
"""

class Solution:
    """
    @param root: the given tree
    @param v: the target value
    @return: the root TreeNode after splitting
    """
    def split_b_s_t(self, root: TreeNode, v: int) -> TreeNode:
        # write your code here
        def splitBST(node: TreeNode, target: int):
            if node is None:
                return None, None
            
            if node.val <= target:
                lower, upper = splitBST(node.right, target)
                node.right = lower
                return node, upper
            else:
                lower, upper = splitBST(node.left, target)
                node.left = upper
                return lower, node

        def count_nodes(node: TreeNode) -> int:
            if node is None:
                return 0
            return count_nodes(node.left) + count_nodes(node.right) + 1

        lower_tree, upper_tree = splitBST(root, v)

        count_lower_nodes = count_nodes(lower_tree)
        count_upper_nodes = count_nodes(upper_tree)

        if count_lower_nodes > count_upper_nodes:
            return lower_tree
        return upper_tree
```
ものすごく時間がかかった(40分くらい?)  
`AC`  
分割しながら、ノード数をカウントしていく方針も考えた  
が、もっとうまく書く方法があると思う  
一度に登場する変数が多くなり、自分としてはコード1の方が読みやすいかな...と感じた  
一回の操作で完了するのは良い点なのだけれども  

### 書いたコード 2
```python
from lintcode import (
    TreeNode,
)

"""
Definition of TreeNode:
class TreeNode:
    def __init__(self, val):
        self.val = val
        self.left, self.right = None, None
"""

class Solution:
    """
    @param root: the given tree
    @param v: the target value
    @return: the root TreeNode after splitting
    """
    def split_b_s_t(self, root: TreeNode, v: int) -> TreeNode:
        # write your code here
        def count_nodes(node: TreeNode) -> int:
            if node is None:
                return 0
            return count_nodes(node.left) + count_nodes(node.right) + 1

        def splitBST(node: TreeNode, target: int):
            if node is None:
                return None, 0, None, 0
            
            if node.val <= target:
                lower, count_lower_nodes, upper, count_upper_nodes = splitBST(node.right, target)
                node.right = lower
                count_lower_nodes += count_nodes(node.left) + 1
                return node, count_lower_nodes, upper, count_upper_nodes
            else:
                lower, count_lower_nodes, upper, count_upper_nodes = splitBST(node.left, target)
                node.left = upper
                count_upper_nodes += count_nodes(node.right) + 1
                return lower, count_lower_nodes, node, count_upper_nodes

        lower_tree, count_lower_nodes, upper_tree, count_upper_nodes = splitBST(root, v)

        if count_lower_nodes > count_upper_nodes:
            return lower_tree
        return upper_tree
```

## Step.1
他の方々のPRを拝見して勉強する  

どうやら、LeetCodeとLintCodeで問題の内容が若干異なるらしい  
今回はLintCodeのバージョンを解いた方のPRを拝見する  

https://github.com/h1rosaka/arai60/pull/62/changes  
stackを使って、itarativeなDFSで解いていた  
setattrを使用していたが、リフレクションと呼ばれる類のものは、  
バグの原因となったり、処理が重い場合があり、原則避けたほうが無難とのこと。

https://github.com/kitano-kazuki/leetcode/blob/776-split-bst/memo.md  
木の分割・削除・ノードのカウントと個別にヘルパー関数として実装していた  
また、`deepcopy`を用いた解法もおこなっていた  
木のコピーを作り、不要な部分をそぎ落としていく感じ  
`deepcopy`についてよく知らなかったので、調べてみた  
https://docs.python.org/ja/3.13/library/copy.html  
今回で言えば、あるノードについて、その子についても全てコピーするもの  

goto-untrapped/Arai60#54
>「自分で手作業でできる」「人間にやり方を説明して代わりにやってもらえる」「機械にやり方を説明して代わりにやってもらえる(おおまかに、これがコードが書けること)」の順で難しくなっていくので、より簡単なのができないのだとたぶん書けないのです。

https://github.com/t9a-dev/LeetCode_arai60/pull/47/files#r2824014277
>もしかすると、上の再帰と合わせてワンパスにして欲しいのかもしれないと思いましたが、私は過剰な最適化と感じます。

Satorien/LeetCode#60
>ワンパスでやるならば、split_bst_helper がサイズも返すようにすればいいんですが……。 まあ、速度は速くなるかもしれませんが、複雑さに見合わない気がしますね。

やっぱり、ノードを数える部分も無理にまとめてやろうとしない方が、可読性の観点から良いといえる  

## Step.2
コード1を見ずにミスなく3回書く  
```python
from lintcode import (
    TreeNode,
)

"""
Definition of TreeNode:
class TreeNode:
    def __init__(self, val):
        self.val = val
        self.left, self.right = None, None
"""

class Solution:
    """
    @param root: the given tree
    @param v: the target value
    @return: the root TreeNode after splitting
    """
    def split_b_s_t(self, root: TreeNode, v: int) -> TreeNode:
        # write your code here
        def splitBST(node: TreeNode, target: int):
            if node is None:
                return None, None
            
            if node.val <= target:
                lower, upper = splitBST(node.right, target)
                node.right = lower
                return node, upper
            else:
                lower, upper = splitBST(node.left, target)
                node.left = upper
                return lower, node

        def count_nodes(node: TreeNode) -> int:
            if node is None:
                return 0
            return count_nodes(node.left) + count_nodes(node.right) + 1

        lower_tree, upper_tree = splitBST(root, v)

        count_lower_nodes = count_nodes(lower_tree)
        count_upper_nodes = count_nodes(upper_tree)

        if count_lower_nodes > count_upper_nodes:
            return lower_tree
        return upper_tree
```


